# Base URL of the deployed backend API (no trailing slash).
VITE_API_URL=http://127.0.0.1:8001

# Base WebSocket URL of the deployed backend (ws:// locally, wss:// in production).
VITE_WS_URL=ws://127.0.0.1:8001
